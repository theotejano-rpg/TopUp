@{
    ViewData["Title"] = "Login";
    Layout = "_Layout";
}

<div class="row justify-content-center mt-4">
    <div class="col-sm-12 col-md-6 col-lg-5">
        <div class="card shadow-sm">
            <div class="dash-card-header text-center">🔑 Login</div>
            <div class="card-body p-4">

                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">❌ @ViewBag.Error</div>
                }

                <p class="text-muted small text-center mb-4">Log in to access your dashboard and manage top-ups.</p>

                <form asp-action="Login" method="post">

                    <div class="mb-3">
                        <label class="form-label">👤 Username</label>
                        <input type="text" name="username" class="form-control" placeholder="Enter username" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">🔒 Password</label>
                        <input type="password" name="password" class="form-control" placeholder="Enter password" />
                    </div>

                    <div class="mt-4 d-flex gap-2">
                        <button type="submit" class="btn btn-pink w-100">🔑 Login</button>
                    </div>

                </form>

                <div class="mt-3 text-center">
                    <small class="text-muted">Don't have an account? <a asp-controller="Account" asp-action="Register" class="fw-bold" style="color:#ec4899;">Create one here</a></small>
                </div>

            </div>
        </div>
    </div>
</div>
